#pragma once

namespace nnw {
    /*
    struct Synapse {
        FloatT weight;

        struct {
            Neuron* back;
            Neuron* front;
        } connections;

        size_t id;
    };
     */
}